import React from "react";

import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export const FontAwesomeIcons = ({icon}) => {
  return <FontAwesomeIcon style={{ fontSize: "12px" }} icon={icon} />;
};
